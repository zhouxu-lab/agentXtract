# Dependencies required by the automated unit/regression suite.
#
# The production parser dependency (docling) is intentionally excluded here:
# it is imported lazily by src/pdf_parser.py, its adapter logic is exercised
# with deterministic fixtures, and installing its GPU-capable Torch stack adds
# several gigabytes without enabling a corpus-backed parser integration test.
# Use requirements-lock.txt for production runs and local end-to-end parsing.
anthropic>=0.40.0
openai>=1.50.0
google-genai>=1.0.0
rich>=13.0.0
pydantic>=2.5.0
pyyaml>=6.0
python-dotenv>=1.0.0
httpx>=0.27.0
duckdb>=1.0.0
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.10.0
pyarrow>=15.0.0
Pillow>=10.0.0
PyMuPDF>=1.24.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
openpyxl>=3.1.0
